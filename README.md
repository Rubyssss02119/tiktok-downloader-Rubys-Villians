---
title: TikTok Downloader — no watermark
emoji: 🎵
colorFrom: pink
colorTo: red
sdk: docker
app_port: 7860
pinned: false
license: mit
---

TikTok Downloader tanpa watermark (ssstik + yt-dlp fallback). Paste link → preview besar → download HD/MP3. Proxy `/download` bypass Access Denied.

API: `GET /health`, `GET /api?url=LINK`, `GET /download?url=...`, `GET /` → UI.
